# QAnything更新说明：

## 2024.07.25 —— 基本功能更新：
* 新增功能：
  * 新增将split后的docs保存到本地文档中，便于可以查看切片后的文档内容

* 优化功能：
  * 删除接口，不再只设置mysql状态码，直接更改为使用delete，方便删除后可以再次创建相同id号知识库或者文件
  * 删除接口，删除时同时删除本地源文件
  * 上传保存接口，保存时路径增加kb_id，方便一目了然
  * docker-compose中mysql容器使用mysql:latest
  * qingqiu.py 测试新增更多可以设置的变量
  * question_rag_search接口，如果输入kb_ids或question为空，则返回告警2002

* 修复问题：
  * 修复了new_knowledge_base接口，当kb_id存在时，返回的kb-id为空问题

* debug：
  * 测试ocr npu版本，需要使用百度提供的镜像，安装成功后可以使用paddleocr npu，但是速度较慢，比cpu版本更慢，暂时不考虑更换
  * 测试ocr gpu版本，速度快，可以识别图片式pdf，设置环境变量OCR_USE_GPU=1，使用gpu版本，默认使用cpu版本
  * 测试npu onnx模型版本，由于算子问题，仍然导致运行很慢，暂时不考虑更换


## 2024.07.30 —— 基本功能更新：
* 修复问题：
  * 当检索结果只有一个时，不进行rerank，search部分得到的socre为numpy.float，返回结果报json序列化错误。在检索阶段修正socre格式。

* 优化功能：
  * 优化question_rag_search接口中，输出显示result结果，以及无效检索"_FAQ"部分
  * 优化question_rag_search接口，返回内容不再包含embed_version
  * 每次_load_kb_to_memory，显示缓存内的faiss_cache_obj.keys()
  * qingqiu.py 测试新增删除部分可以设置file_id

* debug：
  * 测试faiss_cache_obj在知识库更新、删除、新增时，缓存是否正确更新，测试结果错误，需要修改。在更新知识库和删除知识库时，需要删除缓存，在新增知识库时，需要更新缓存。

* bug:
  * faiss使用缓存策略会造成，当知识库更新时，缓存中的数据没有更新，导致检索结果不准确。需要修改缓存策略，在更新知识库时，删除缓存，在新增知识库时，更新缓存。
  * 问题的原因就是他faiss_client一个单例，里面的FAISS 反复在merge，造成不同kb之间、同一个kb的不同版本之间互相污染



## 2024.08.01 —— 基本功能更新：
* 修复问题：
  * 修复了faiss_cache_obj在知识库更新、删除、新增时，缓存没有正确更新，导致检索结果不准确。在更新知识库和删除知识库时，需要删除缓存，在新增知识库时，需要更新缓存。

* 优化功能：
  * handler_search中，每个接口返回前进行格式化显示输出，方便查看debug

* debug：
  * 测试BMP中，删除无效，名称重复。qanything重启时，BMP操作导致数据不一致。需要定期BMP与数据库进行同步。

* todo:
  * 优化chunk_size，可以自动更改，分段等需要进行改进。


## 2024.08.06 —— 基本功能更新：
* debug:
  * 测试split_text相关功能，根据文档内容，优化split_text，主要是段落换行、步骤不进行split等。
  * 测试pdf文件的load_to_markdown功能，部分能够成功，部分会失败，会根据标题进行切分，标题下字数超出，会按照对应的pdf_text_splitter分片，并对每片增加说明，f'\n######第{idx+1}段内容如下：\n'


## 2024.08.07 —— commit：
* 新增：
  * 在检索结果进行merge时，根据参数设置merge的token长度限制，model_config.py中MERGE_TOKEN_LENGTH
  * 文档解析接口，解析后文件依旧存储到本地了，进行删除。
  * python版本新增paddleocr接口，以及相关启动参数设置


* 修复问题：
  * 部分文件load_to_markdown失败问题，当ocr解析后的json中，text为空时，"string index out of range"问题修复。
  * 使用to_markdown方式加载pdf，PdfLoader接口调用错误，进行修复，save_dir非root_dir

* todo：
  1、测试对比更新后的split_text的实际应用效果，方式：新津所有文档，对比服务器上的检索效果。效果还是有提升，需要打开merge。不打开merge在rerank时，效果不太好。